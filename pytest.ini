[pytest]

asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests for individual components
    integration: Integration tests across services
    asyncio: Async tests
    postgres: Tests requiring a live PostgreSQL connection

addopts =
    --strict-markers
    -v
